Run this from inside classroom-attendance-platform:

  bash classroomops-roster-save-fix/APPLY_ROSTER_FIX.sh .
  npm run test -- --run
  npm run build

The roster count is a preview. Press the blue Create/update button to write accounts. A successful import opens the Student directory automatically.
